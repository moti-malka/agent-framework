# ─── Azure OpenAI via API Management (APIM) ──────────────────────────────────
AZURE_OPENAI_CHAT_DEPLOYMENT_NAME="gpt-4o-mini"
AZURE_OPENAI_ENDPOINT="https://<your-apim-instance>.azure-api.net"
AZURE_OPENAI_API_KEY="<your-apim-subscription-key>"
API_VERSION="2025-01-01-preview"

# ─── Azure AI Agent Service ──────────────────────────────────────────────────
AZURE_AI_PROJECT_ENDPOINT=https://<your-foundry-resource>.services.ai.azure.com/api/projects/<your-project>
AZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-4o-mini

# ─── GitHub Access ───────────────────────────────────────────────────────────
GITHUB_TOKEN=ghp_<your-github-token>

# ─── Service Principal for DefaultAzureCredential (Azure AI Agent Service) ────
# If set, DefaultAzureCredential uses these automatically (no az login needed).
# If unset, it falls back to az login / managed identity.
AZURE_CLIENT_ID="<your-service-principal-client-id>"
AZURE_TENANT_ID="<your-azure-tenant-id>"
AZURE_CLIENT_SECRET="<your-service-principal-secret>"
